```
getFingerprint(file) hash

create(fingerprint, meta) (dataid, txhash)

/ddxf/seller/regIdAndAddDataMeta

update(dataid, fingerprint, meta) txhash

/ddxf/seller/saveDataMeta
/ddxf/seller/saveDataMetaArray

remove(dataid) txhash

get(dataid) ddo
```
